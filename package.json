{
  "name": "dimension",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "watch:core": "cd ./packages/core && npm run watch",
    "watch:react": "cd ./packages/react && npm run react",
    "watch": "run-p watch:*",
    "build:core": "cd ./packages/core && npm run build",
    "build:react": "cd ./packages/react && npm run build",
    "build": "run-s build:*"
  },
  "workspaces": [
    "packages/*",
    "homepage"
  ],
  "author": "Grant Forrest",
  "license": "MIT",
  "devDependencies": {
    "@yarnpkg/pnpify": "^2.0.0-rc.18",
    "eslint": "^6.8.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^1.19.1",
    "typescript": "^3.7.5"
  }
}
